import { Button } from "./components/ui/button";

type Props = {};

const Test = (props: Props) => {
  return (
    <div>
      <Button variant="default">Test</Button>
    </div>
  );
};

export default Test;
